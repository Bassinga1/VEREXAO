<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Le CDN de fa #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        {# La feuille de style de Bootstrap version=5.2 #}
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
        {# La feuille de style du style minifié de lightbox #}
        <link rel="stylesheet" href="{{ asset('css/lightbox.min.css') }}">
        {# La feuille de style du style front minifié de public/scss/style-front.scss #}
        <link rel="stylesheet" href="{{ asset('css/style-front.min.css') }}">
        {# Le font-family Montserrat #}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,400;0,600;1,500&family=Poppins:ital,wght@0,100;0,500;0,600;1,200&display=swap" rel="stylesheet">
        {# Le cdn de https://cdnjs.com/libraries/font-awesome/6.4.0 #}
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        {% block stylesheets %}{% endblock %}
    </head>
    
    <body>
        {% include 'admin_front/_top-nav.html.twig' %}
        <main class="container-xl py-4 shadow bg-white">
        {# On met une alert Boostrap si necessaire #}
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} alert-dismissible fade show">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}
        {% endfor %}
        {% block body %}{% endblock %}
        </main>
        {% include 'admin_front/_footer.html.twig' %}
        {# Le CDN du jquery #}
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script>
        {# Le CDN javascript Bootstrap 5.2 #}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
        {# Le js de lightbox #}
        <script src="{{ asset('js/lightbox.min.js') }}"></script>
        <script src="{{ asset('js/favoris.js') }}"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
